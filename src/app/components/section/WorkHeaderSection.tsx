import styles from "@/app/styles/section.module.scss";

const WorkHeaderSection = () => {
    return (
        <div className={styles.workHeaderSection}>
            <h1>Work</h1>
        </div>
    );
};

export default WorkHeaderSection;
